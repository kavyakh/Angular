import { RverseStringPipe } from './rverse-string.pipe';

describe('RverseStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RverseStringPipe();
    expect(pipe).toBeTruthy();
  });
});
